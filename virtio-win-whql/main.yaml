- project:
    name: 'virtio-win-whql-testing'
    jobs:
        - 'trigger'
        - 'runtest'

- job-group:
    name: 'trigger'
    jobs:
        - 'whql-trigger'

- job-group:
    name: 'runtest'
    jobs:
        - 'whql-{task}'
    task:
        - '2008R2-2012R2-runtest'
